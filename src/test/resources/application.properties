spring.application.name=wishlist

# Conexão MongoDB - Test Profile (usando credenciais do Docker Compose)
# Usa variável de ambiente MONGO_DB_URL se definida, caso contrário usa a URL padrão
spring.mongodb.uri=${MONGO_DB_URL:mongodb://root:secret@localhost:27017/wishlist-test?authSource=admin}
spring.mongodb.database=wishlist-test

# Logging
logging.level.com.berkson.wish.wishlist=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG
